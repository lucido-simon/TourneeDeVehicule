package common.Edge;

import common.Vertex.SweepVertex;

public class SweepEdge extends BasicEdge<SweepVertex>
{
    public SweepEdge(SweepVertex ln1, SweepVertex ln2, double dist)
    {
        super(ln1, ln2, dist);
    }
}
